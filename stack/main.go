package main

import (
	"fmt"
)

type Node struct {
	content interface{}
	up *Node
}

func (s *Node) push(newNode Node) {
	if s.up == nil {
		s = &newMode
	} else {
		
	}
}

func main() {
	fmt.Println("ok")
}
